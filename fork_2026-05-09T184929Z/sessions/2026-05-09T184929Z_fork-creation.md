# Fork Creation: paper_processor.py

**Host:** morlok-AM4
**Working dir:** `/home/jeb/programs/python_programs/paper_processor/fork_2026-05-09T184929Z`
**Started:** 2026-05-09 18:49:29Z
**Status:** Initial Fork Created

## Goal

Create an isolated fork of the `paper_processor.py` script in a timestamped directory to allow for independent experimentation and session tracking.

## What was accomplished

1.  **Directory Creation:** Created `fork_2026-05-09T184929Z/` and `fork_2026-05-09T184929Z/sessions/`.
2.  **Script Replication:** Copied the original `paper_processor.py` from the root directory into the fork directory.
3.  **Session Initialisation:** Generated this initial session document to establish the fork's history.
4.  **VRAM Constraint Refactor:**
    - Restricted all models to ≤ 8.9 GB VRAM.
    - Set `deepseek-r1:8b` (5.2 GB) as the primary reasoning/summary model.
    - Set `ministral-3:8b` (6.0 GB) as the code model.
    - Set `qwen2.5:3b` (1.9 GB) for short papers (≤ 10 pages).
    - Updated shebang to `../.venv/bin/python` for proper path resolution from the subdirectory.
    - Updated documentation/help text to reflect these constraints.
5.  **Parallel Performance Enhancement:**
    - Refactored `map_reduce_chunks` to process chunks in parallel using `ThreadPoolExecutor`.
    - Integrated with `OLLAMA_NUM_PARALLEL` environment variable (defaults to 2).
    - **Multi-GPU Worker Scaling & Model Residency:**
        - Implemented strict GPU pinning by targeting dual Ollama instances (Ports 11434 and 11435).
        - **GPU-Specific Model Fitness:**
            - **GPU 0 (3080, 10GB):** Paired `deepseek-r1:8b` (reasoning) with `qwen3.5:4b` (code) to ensure both stay hot-loaded within the 10GB VRAM limit.
            - **GPU 1 (3060, 12GB):** Paired `deepseek-r1:8b` (reasoning) with `ministral-3:8b` (code), utilizing the larger 12GB buffer for high-quality code.
        - Verified concurrent processing across both GPUs with zero model evictions during interleaved reasoning/code tasks.

## Environment Details

- **Original Script:** `/home/jeb/programs/python_programs/paper_processor/paper_processor.py`
- **Forked Script:** `./paper_processor.py`
- **Python Venv:** Uses the venv in the parent directory (`../.venv`).
- **Default Papers Dir:** `/home/jeb/Documents/AI-ML_Papers`
- **Model Residency:** Multi-model "hot-loading" enabled via dual Ollama ports and GPU-specific model selection.
- **Parallelism:** Supported at chunk and paper levels across dual hardware backends.

## Next Steps

- Monitor VRAM usage during long paper processing to ensure stability with parallel chunks on the smaller 3060 (12GB).
