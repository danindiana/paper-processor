# Session Log - 2026-05-15T235801Z

## Objective
Create a new fork and implement optimizations for Ollama VRAM scheduling to improve performance and throughput.

## Background
Logs indicate Ollama is experiencing VRAM scheduling churn when loading multiple large models or large context buffers on a mixed-GPU setup (RTX 5080 + RTX 3080).

## Optimization Goals
1. Implement single-runner policy where appropriate.
2. Optimize context length management.
3. Enable Flash Attention.
4. Use Q8_0 KV cache if supported.
5. Control concurrency via `OLLAMA_MAX_LOADED_MODELS` and `OLLAMA_NUM_PARALLEL`.

## Actions Taken
- Initialized fork: `fork_2026-05-15T235801Z`
- Researched `paper_processor.py` and identified hardcoded context lengths and lack of fine-grained Ollama options.
- Implemented the following in `fork_2026-05-15T235801Z/paper_processor.py`:
    - Updated Hardware Context comments for RTX 5080 (16GB) + RTX 3080 (10GB).
    - Updated `MODEL_TIERS` to use models already present on the system:
        - `xl_quality`: `qwen3.6:35b` (23 GB)
        - `xl_reason`: `deepseek-r1:32b` (19 GB)
        - `xl_code`: `qwen3-coder:30b` (18 GB)
        - `mid_reason`: `deepseek-r1:14b-qwen-distill-q8_0` (15 GB)
        - `mid_code`: `qwen2.5-coder:14b-base-q6_K` (12 GB)
        - `single`: `deepseek-r1:14b` (9 GB)
        - `fast`: `deepseek-r1:8b` (5.2 GB)
    - Refactored `Backend` class to store and pass performance-tuning options.
    - Added CLI flags: `--ctx`, `--no-flash`, `--kv-cache`, and `--single-runner`.
    - Enabled Flash Attention by default and set default context to 8192 for stability.
    - Implemented `single_runner` mode using `keep_alive=0` to force immediate model eviction.
- Updated documentation in the fork's `README.md` including recommended `systemd` override settings.
- Added the fork to the main project `README.md`.

## Verification
- Script loads and parses arguments correctly.
- `Backend` correctly passes options to Ollama API in `_call_ollama`.
- Hardcoded 16k/32k context lengths removed in favor of global `--ctx` setting.
